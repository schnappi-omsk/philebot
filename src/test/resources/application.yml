spring:
  main:
    allow-bean-definition-overriding: true
  datasource:
    url: jdbc:postgresql://testHost:testPort/testDb
    username: pgUser
    password: pgPass
  flyway:
    baseline-on-migrate: false
    baseline-version: 1
xapi:
  baseUrl: https://localhost
  apiToken: 123456
tg:
  baseUrl: https://localhost
  apiToken: 123456
  chatId: -123456
  botName: Phil E-Bot
  systemCaller: Phil Spencer
  systemArg: Power your dreams
ebot:
  serviceHost: testHost
  limitPerUser: 5
redis:
  host: localhost
  port: 6379
  user:
  pass:
server:
  servlet:
    context-path: /
