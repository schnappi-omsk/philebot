spring:
  main:
    allow-bean-definition-overriding: true
  jpa:
    hibernate:
      ddl-auto: none
  datasource:
    url: jdbc:h2:mem:testdb;DB_CLOSE_DELAY=-1
    username: pgUser
    password: pgPass
  flyway:
    baseline-on-migrate: false
    baseline-version: 1
xapi:
  baseUrl: https://localhost
  apiToken: 123456
tg:
  baseUrl: https://localhost
  apiToken: 123456
  botName: Phil E-Bot
  systemCaller: Phil Spencer
  systemArg: Power your dreams
ebot:
  serviceHost: testHost
  limitPerUser: 5
  requestsPerMin: 10.0
server:
  servlet:
    context-path: /
messages:
  periodical: Мда, Руслан, конечно, не шарит...
  congrats: Разъебывает